// Backend API Configuration
// Update this when deploying frontend separately from backend

// For local development:
// window.BACKEND_URL = 'http://localhost:3000';

// For split deployment (frontend on Vercel, backend elsewhere):
// window.BACKEND_URL = 'https://your-backend-url.com';

// For Oracle Cloud backend:
// window.BACKEND_URL = 'http://xxx.xxx.xxx.xxx:3000';

// For Ngrok:
// window.BACKEND_URL = 'https://abc123.ngrok.io';

// For Replit backend:
// window.BACKEND_URL = 'https://your-app.replit.app';

// Default: same server (comment out for split deployment)
// window.BACKEND_URL = undefined;
